package cl.recoders.directorio.repository;

import cl.recoders.directorio.entity.RegistroEstado;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RegistroEstadoRepository extends JpaRepository<RegistroEstado, Integer> {
}